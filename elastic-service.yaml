apiVersion: v1
kind: Service
metadata:
  name: elastic-service
  namespace: data-as-a-service-hl-global
  labels:
    app: elk-logging
spec:
  ports:
  - port: 9200
    protocol: TCP
    targetPort: db
  selector:
    app: elk-logging