apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: promtail-claim2
  name: promtail-claim2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
