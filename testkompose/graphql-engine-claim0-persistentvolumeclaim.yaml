apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: graphql-engine-claim0
  name: graphql-engine-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
